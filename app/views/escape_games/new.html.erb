<h1>New Escape Game</h1>

<div class="card">
    <div class="card-body w-75 d-flex flex-row mx-auto">
        <%= render 'form', escape_game: @escape_game %>
    </div>
</div>

<%= link_to 'Back', escape_games_path %>